# תרומה לפרויקט

תודה שאתה מסתכל. הפרויקט קטן בכוונה, וקל להוסיף לו דברים.

## להתחיל

```bash
git clone <כתובת הריפו>
cd daily-summary
pip install -r requirements.txt pytest
cp config.example.yaml config.yaml
```

כדי לבדוק בלי מפתח API ובלי לשלוח כלום, שנה ב-`config.yaml` ל-`provider: mock` והרץ:

```bash
python -m src.main --dry-run --force
```

הבדיקות:

```bash
python -m pytest tests -q
```

## להוסיף סוג מקור חדש

זה החלק שהכי סביר שתרצה לגעת בו. שלושה שלבים ב-[src/fetchers.py](src/fetchers.py):

1. כתוב פונקציה `fetch_<type>(src, limit, cache)` שמחזירה רשימת `Item`. שים תאריך ב-`published` כדי שחלון הטריות יעבוד.
2. אם התוכן דורש שליפה נוספת (כמו תמלול), כתוב `load_<type>_content(item)` שממלא את `item.content`.
3. רשום את שתיהן ב-`FETCHERS` וב-`LOADERS`.

אחר כך הוסף את הסוג ל-`SOURCE_TYPES` ב-[setup.html](setup.html) כדי שיופיע בטופס.

## להוסיף ספק מודל שפה

ב-[src/llm.py](src/llm.py): פונקציה שמקבלת `(prompt, model)` ומחזירה מחרוזת, ורישום ב-`PROVIDERS`. הוסף גם ל-`DEFAULT_MODEL` ו-`KEY_HELP` בטופס.

## להוסיף יעד שליחה

ב-[src/deliver.py](src/deliver.py): פונקציה שמקבלת `(text, target)` ומחזירה `True` בהצלחה, ורישום ב-`SENDERS`.

## כללים

- **מקור או יעד שנופל לא מפיל את הריצה.** תפוס חריגות, כתוב ללוג, המשך הלאה.
- **אל תמציא תוכן.** הכלל המרכזי בפרויקט: הסיכום מבוסס רק על מה שנאמר במקור. אם אין תוכן — מדלגים.
- **סודות ב-`.env` בלבד.** אף פעם לא בקוד ולא ב-`config.yaml`.
- **בדיקה לכל התנהגות שיכולה להישבר בשקט.** פענוח תאריכים, פיצול הודעות, יציבות מזהים.
- הודעות ללוג בעברית, שמות משתנים ופונקציות באנגלית — כמו בשאר הקוד.

## שליחת שינוי

עשה fork, פתח ענף, ופתח pull request. הבדיקות ירוצו אוטומטית. אם השינוי גדול, שווה לפתוח issue קודם.

הפרויקט ברישיון MIT — בשליחת שינוי אתה מסכים שהוא ישוחרר תחת אותו רישיון.
